name: sum25_flutter_frontend
description: Summer 2025 Go + Flutter Course - Frontend Application
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'
  flutter: '>=3.32.1'

dependencies:
  flutter:
    sdk: flutter
  
  # HTTP client for API communication
  http: ^1.2.2
  
  # State management (Riverpod + Bloc)
  flutter_riverpod: ^3.3.2-dev.2
  riverpod_annotation: ^4.0.3-dev.2
  flutter_bloc: ^9.1.1
  
  # gRPC client support
  grpc: ^5.1.0
  protobuf: ^6.0.0
  
  # Local storage
  shared_preferences: ^2.3.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  
  # WebSocket support
  web_socket_channel: ^3.0.1
  
  # JSON serialization
  json_annotation: ^4.9.0
  
  # Navigation
  go_router: ^17.3.0
  
  # UI components
  cupertino_icons: ^1.0.8
  
  # Analytics and monitoring
  firebase_analytics: ^12.4.2
  firebase_crashlytics: ^5.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # Code generation
  build_runner: ^2.4.13
  json_serializable: ^6.8.0
  riverpod_generator: ^4.0.4-dev.3
  riverpod_lint: ^3.1.4-dev.3
  
  # Linting
  flutter_lints: ^6.0.0
  
  # Testing
  mockito: ^5.4.4
  bloc_test: ^10.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  
  # Assets (add when needed)
  # assets:
  #   - images/
  
  # Fonts (add when needed)
  # fonts:
  #   - family: CustomFont
  #     fonts:
  #       - asset: fonts/CustomFont-Regular.ttf 